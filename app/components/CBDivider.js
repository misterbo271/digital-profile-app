import React from 'react';
import {Divider} from 'react-native-elements';

const CBDivider = (props) => {
    return (
        <Divider {...props} style={props.style}/>
    );
};

export default CBDivider;

