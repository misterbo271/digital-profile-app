import React from 'react';
import {ButtonGroup} from 'react-native-elements';

const CBButtonGroup = (props) => {
    return (
        <ButtonGroup {...props}/>
    );
};

export default CBButtonGroup;

