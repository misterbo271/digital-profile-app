import React from 'react';
import {LinearProgress} from 'react-native-elements';

const CBLinearProgress = (props) => {
    return (
        <LinearProgress {...props}/>
    );
};

export default CBLinearProgress;
