import React from 'react';
import {BottomSheet} from 'react-native-elements';

const CBBottomSheet = (props) => {
    return (
        <BottomSheet {...props}/>
    );
};

export default CBBottomSheet;

